[build]
  publish = "frontend"

[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
  conditions = {Role = ["admin"]}

# Serve login.html for unauthenticated routes
[[redirects]]
  from   = "/"
  to     = "/login.html"
  status = 302
